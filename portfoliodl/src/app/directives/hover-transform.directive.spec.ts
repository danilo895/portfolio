import { HoverTransformDirective } from './hover-transform.directive';

describe('HoverTransformDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverTransformDirective();
    expect(directive).toBeTruthy();
  });
});
